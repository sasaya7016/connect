#sidebar.nav-collapse.tweet-sidebar
  / sidebar menu start
  = link_to root_path do
    %p.management-title Connect
 
    .my-image
      -if current_user.icon_image.url.nil?
      -else
        = image_tag(current_user.icon_image.url)
    .my-name
      = current_user.nickname
    %br
    
  %ul#nav-accordion.sidebar-menu
    = link_to root_path,class:"mm-title" do
      = icon('fas', 'bars', class: 'icon')
      Home / メニュー
  

    %li.mt
      = link_to root_path do
        = icon('fas', 'pen-square', class: 'icon')
        %span Home / ツイートリスト
      
    %li.sub-menu
      = link_to groups_path do
        = icon('far', 'comment', class: 'icon')
        %span Chat / チャット

    %li.sub-menu
      = link_to "#" do
        = icon('fas', 'globe-asia', class: 'icon')
        %span World / UserList(未実装)

    %hr 
  
  %ul#nav-accordion.sidebar-menu
    = link_to  user_path(current_user.id),class:"mm-title" do
      = icon('fas', 'user', class: 'icon')
      My page / メニュー

    %li.sub-menu
      = link_to  user_path(current_user.id) do
        = icon('fas', 'pen-square', class: 'icon')
        %span Profile / マイツイート

    %li.sub-menu
      = link_to "#" do
        = icon('fas', 'users', class: 'icon')
        %span Follow /My Follower(未実装)
      

  -#  %li
  -#     %a.active{:href => "inbox.html"}
  -#       %i.fa.fa-envelope
  -#       %span Mail
  -#       %span.label.label-theme.pull-right.mail-info 3