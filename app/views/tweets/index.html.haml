
  
= render "tweets/modules/header"
= render "tweets/modules/sidebar"


.review
  .review__tweets
    %h1 投稿
    %p お店の商品や利用していただいた方へ
    %p ご要望・ご指摘がございましたら、ご記入ください。


.review-lists
  -@tweets.each do |tweet|
    .review-lists__list
      .user_image
        .my-image
          =link_to image_tag("gest.fujita.jpeg",class:"top-logo")
          -# = image_tag tweet.image.url
        .my-name
          = tweet.user.nickname
      .user_text
        .date-time
          = tweet.created_at.strftime('%Y/%m/%d %H:%M')
          .link
            = link_to '詳細', "/tweets/#{tweet.id}", method: :get 
            -if user_signed_in? && current_user.id == tweet.user_id
              = link_to '編集', "/tweets/#{tweet.id}/edit", method: :get
              = link_to '削除', "/tweets/#{tweet.id}", method: :delete 

        .user_name
          = simple_format tweet.text

        .kuchikomi
          ■
        .like
          ♡
          
     
  = paginate(@tweets)
           
            
= render "tweets/modules/subbar"