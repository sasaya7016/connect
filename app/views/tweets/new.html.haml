.review-form
  = form_with(model: @tweet, local: true) do |f|
    = f.text_field :name, placeholder: "Nickname"
    = f.file_field :image, placeholder: "Image Url"
    = f.text_area :text, placeholder: "text" , rows: "120" 
    = f.submit "SEND"
 