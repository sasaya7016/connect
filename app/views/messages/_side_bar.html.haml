.sub-chat-bar
  .connect-groups
    グループリスト
    - current_user.groups.each do |group|
      .connect-groups__lists
        .connect-groups__lists__name
          %h3
            = link_to group_messages_path(group) do
              %p.group-name
                = group.name
              %p.group-edit 
                = link_to edit_group_path(group) do
                  = icon('fas', 'object-group', class: 'icon')
        .connect-groups__lists__member   
          %p.header__group__member 
            - group.users.each do |user|
              = group.show_last_message
              -# = user.nickname




  
